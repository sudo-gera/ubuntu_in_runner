[
  "sudo apt update || apt update",
  "sudo apt install -y sudo || apt install -y sudo",
  "DEBIAN_FRONTEND=noninteractive TZ=Etc/UTC sudo apt-get -y install tzdata",
  "sudo apt install -y software-properties-common",
  "sudo add-apt-repository ppa:mozillateam/ppa",
  "sudo apt install -y bind9-host",
  "sudo apt install -y curl",
  "sudo apt install -y dbus-x11",
  "sudo apt install -y dbus",
  "sudo apt install -y dnsutils",
  "sudo apt install -y firefox-esr",
  "sudo apt install -y git",
  "sudo apt install -y gpg",
  "sudo apt install -y iproute2",
  "sudo apt install -y iputils-ping",
  "sudo apt install -y lsb-release",
  "sudo apt install -y lsof",
  "sudo apt install -y nano",
  "sudo apt install -y net-tools",
  "sudo apt install -y openssh-client",
  "sudo apt install -y openssh-server",
  "sudo apt install -y passwd",
  "sudo apt install -y python3",
  "sudo apt install -y snapd",
  "sudo apt install -y squid",
  "sudo apt install -y sudo",
  "sudo apt install -y systemd-sysv",
  "sudo apt install -y systemd",
  "sudo apt install -y tigervnc-common",
  "sudo apt install -y tigervnc-standalone-server",
  "sudo apt install -y tightvncserver",
  "sudo apt install -y tmate",
  "sudo apt install -y tmux",
  "sudo apt install -y unzip",
  "sudo apt install -y wget",
  "sudo apt install -y wireguard-tools",
  "sudo apt install -y wireguard",
  "sudo apt install -y x11vnc",
  "sudo apt install -y xfce4-goodies",
  "sudo apt install -y xfce4-session",
  "sudo apt install -y xfce4",
  "sudo apt install -y xrdp",
  "sudo apt install -y xxd",
  "sudo apt install -y adb",
  "sudo apt install -y scrcpy",
  "mkdir -p ~/.vnc/",
  "echo | vncpasswd -f | tee ~/.vnc/passwd > /dev/$1,",
  "chmod 600 ~/.vnc/passwd",
  "touch ~/.Xauthority",
  "echo 'unset SESSION_MANAGER' | tee -a ~/.vnc/xstartup > /dev/$1,",
  "echo 'unset DBUS_SESSION_BUS_ADDRESS' | tee -a ~/.vnc/xstartup > /dev/$1,",
  "echo 'startxfce4' | tee -a ~/.vnc/xstartup > /dev/$1,",
  "chmod +x ~/.vnc/xstartup",
  "curl -L https://github.com/novnc/noVNC/archive/refs/tags/v1.6.0.zip -o ~/novnc.zip",
  "unzip ~/novnc.zip",
  "mv ./noVNC* ~/novnc",
  "printf '%s\\n' 'export USER=$(whoami)' | tee -a ~/.bashrc > /dev/$1,",
  "( ~/novnc/utils/novnc_proxy &) ; while sleep 0.1 ; do curl 127.0.0.1:6080 && break ; done",
  "mkdir -p ~/.ssh/",
  null,
  "./to_be_launched_in_runner.sh"
]
